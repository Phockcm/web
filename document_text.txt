
HỌC PHẦN	: LẬP TRÌNH JAVASCRIPT
SỐ TÍN CHỈ	: 	3TC (30 tiết LT + 40 tiết TH/1SV)
GIẢNG VIÊN	: 	BỘ MÔN CÔNG NGHỆ PHẦN MỀM
BUỔI 9. DỰNG UI
Mục tiêu
Hiểu Layout – Component – Page
Biết dựng UI 
Câu 1. Tạo Layout Shop & Admin
src/
├── app/
│   ├── (shop)/
│   │   ├── layout.jsx     👈 Header + Footer
│   │   ├── page.jsx
│   │   ├── products/
│   │   └── cart/
│   │
│   ├── admin/
│   │   ├── layout.jsx     👈 Sidebar + AdminHeader
│   │   ├── page.jsx
│   │   └── products/
│   │
│   └── layout.jsx         # root (html, body)
│   └── global.css         # root (html, body)
│
├── components/
├──  data/ 		     # chứa dữ liệu mẫu
├── menu.js     
├── products.js
└── adminMenu.js
└── product.js
File global.css   giảng viên cho trước
Root Layout
import "./globals.css";
export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
ShopLayout
import Header from "@/components/shop/Header";
import Footer from "@/components/shop/Footer";
export default function ShopLayout({ children }) {
  return (
    <>
      <Header />
      <main>{children}</main>
      <Footer />
    </>
  );
}
AdminLayout
import AdminHeader from "@/components/admin/AdminHeader";
import Sidebar from "@/components/admin/Sidebar";
import Footer from "@/components/shop/Footer";
export default function AdminLayout({ children }) {
    return (
        <>
            <AdminHeader/>
            <div style={{ display: "flex" }}>
                <div style={{ width: 200 }}>
                    <Sidebar/>
                </div>
                <main style={{ padding: 20 }}>{children}</main>
            </div>
            <Footer/>
        </>
    );
}
Dữ liệu mẫu:
menu.js
export const shopMenu = [
    { label: "Trang chủ", href: "/" },
    { label: "Sản phẩm", href: "/products" },
    { label: "Bài viết", href: "/posts" },
    { label: "Liên hệ", href: "/contact" },
];
adminMenu.js
export const adminMenu = [
    { label: "Dashboard", href: "/admin" },
    { label: "Products", href: "/admin/products" },
    { label: "Categories", href: "/admin/categories" },
    { label: "Orders", href: "/admin/orders" },
];
products.js
export const products = [
 { id: 1, name: "iPhone 15 Pro", price: 25990000, image: "iphone15.jpg", category: "Phone" },
    { id: 2, name: "iPhone 14", price: 18990000, image: "iphone14.jpg", category: "Phone" },
];
product.js
export const product ={ id: 2, name: "iPhone 14", price: 18990000, image: "iphone14.jpg", category: "Phone" } ;
Câu 2. Tạo cấu trúc cây folder component
components/
├── common/                         # Component dùng chung
│   ├── Button.jsx                  # Button
│   ├── Card.jsx                    # Card
│   ├── Table.jsx                   # Table
│   ├── Input.jsx                   # Input
│   ├── Modal.jsx                   # Modal
│   └── Empty.jsx                   # Empty
│
├── shop/                           # Component phía SHOP
│   ├── Header.jsx                  # Header (Logo+Menu+Cart icon)
│   ├── Footer.jsx                  # Footer
│   ├── Menu.jsx                    # Menu động
│   │
│   ├── product/                    # Nhóm component sản phẩm
│   │   ├── ProductCard.jsx         # ProductCard
│   │   └── ProductList.jsx         # ProductList
│   │   └── ProductDetail.jsx         # ProductList
│   │
│   └── cart/                       # Nhóm component giỏ hàng
│       └── CartItem.jsx            # CartItem
│
└── admin/                          # Component phía ADMIN
    ├── Sidebar.jsx                 # Sidebar
    ├── AdminHeader.jsx             # AdminHeader
    ├── AdminButton.jsx             # AdminButton
    │
    └── table/                      # Nhóm bảng admin
        └── AdminTable.jsx          # AdminTable (CRUD)
Câu 3. Nội dung các component common
2.1 Button
export default function Button({ children }) {
  return <button>{children}</button>;
}
2.2 Card
export default function Card({ children }) {
  return <div className="card">{children}</div>;
}
2.3 Table 
export default function Table({ columns, data }) {
  return (
    <table border="1">
      <thead>
        <tr>
          {columns.map(col => (
            <th key={col.key}>{col.label}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {data.map(row => (
          <tr key={row.id}>
            {columns.map(col => (
              <td key={col.key}>{row[col.key]}</td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  );
}
2.4 Input
export default function Input({ value, onChange, ...rest }) {
  return <input value={value} onChange={onChange} {...rest} />;
}
2.5 Modal
export default function Modal({ title, children }) {
  return (
    <div className="modal-overlay">
      <div className="modal">
        <header className="modal-header">
          <h3>{title}</h3>
        </header>
        <div className="modal-body">
          {children}
        </div>
      </div>
    </div>
  );
}
2.6 Empty
export default function Empty({ message = "Không có dữ liệu" }) {
  return (
    <div className="empty">
      <p>{message}</p>
    </div>
  );
}
Câu 4. Xây dựng Component SHOP
4.1 Header – Menu – Footer
Header hiển thị:
Logo
Menu động
Cart icon (UI)
4.2 ProductCard,ProductList, ProductDetail
ProductCard: nhận vào 1 product và hiển thị 1 sản phẩm dạng Card
ProductList: nhận mảng products và render danh sách ProductCard tương ứng
ProductDetail: hiển thị chi tiết 1 sản phẩm
Câu 5. Xây dựng Component ADMIN
5.1 AdminTable
Dùng lại Table từ common
Bổ sung cột Action (Sửa / Xóa)
5.2 Sidebar & AdminHeader
Sidebar dùng menu động từ adminMenu.js
AdminHeader hiển thị tên admin + nút đăng xuất (UI)
Câu 6. Dựng UI Tĩnh cho Page
6.1 ProductsPage (Shop)
Import dữ liệu mẫu từ data/products.js
Truyền dữ liệu vào ProductList
6.2 ProductDetailPage (Shop)
Import dữ liệu mẫu từ data/product.js
Truyền dữ liệu vào component ProductDetail
6.3 Admin Products Page
Dùng AdminTable
Hiển thị danh sách sản phẩm tĩnh
Câu 7. Định dạng các component bằng tailwind css (sv tự làm)